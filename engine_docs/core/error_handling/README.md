# Error Handling

Engine error codes, diagnostics macros, condition checks, and error message reporting.

## Subtopics

- [Error Codes And Macros](error_codes_and_macros.md): `Error` values and `ERR_*` / `WARN_*` diagnostics macros.

## Global Entry Points

- `Error` is the shared error enum returned by many engine APIs.
- Error macros provide consistent failure, warning, and condition-reporting behavior.

## Code Links

- `core/error/error_list.*`
- `core/error/error_macros.*`